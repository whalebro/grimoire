# Site settings
title: "Grimoire"
author: "whalebro(fork creator)"
description:
  "A searchable D&D 5e spell list, sorted by class and level."
baseurl: "/grimoire" # the subpath of your site, e.g. /blog/
url: "https://whalebro.github.io" # the base hostname & protocol for your site
source-url: "https://github.com/whalebro/grimoire/"
permalink: "/:categories/:title"
exclude: ['Gemfile', 'Gemfile.lock', 'Rakefile', 'README.md']

# Build settings
markdown: kramdown
highlighter: pygments

# Site defaults
defaults:
  -
    scope:
      path: ""
      type: "posts"
    values:
      category: "spells"
